╔════════════════════════════════════════════════════════════════════════════╗
║                    QUICK START CHECKLIST                                  ║
║                    SimuAI Platform - Get Running in 5 Minutes              ║
╚════════════════════════════════════════════════════════════════════════════╝

STEP 1: START POSTGRESQL DATABASE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☐ Double-click: START_POSTGRES_NOW.bat
  (Wait for: ✅ PostgreSQL is running and database is accessible!)

OR manually:
☐ Press: Win + R
☐ Type: services.msc
☐ Find: PostgreSQL
☐ Right-click → Start
☐ Wait for status: Running

STEP 2: OPEN FIRST TERMINAL - START SERVER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☐ Open Command Prompt
☐ Run: cd server
☐ Run: npm run dev
☐ Wait for: "Server running on port 5000"

STEP 3: OPEN SECOND TERMINAL - START CLIENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☐ Open NEW Command Prompt
☐ Run: cd client
☐ Run: npm run dev
☐ Wait for: "Compiled successfully!"

STEP 4: OPEN APPLICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☐ Open browser
☐ Go to: http://localhost:3000
☐ You should see the SimuAI homepage

STEP 5: TEST FEATURES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☐ Register a new account
☐ Login with your credentials
☐ Browse jobs
☐ Click chatbot button (bottom-right) and test AI responses
☐ Explore dashboards

TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ "Can't reach database server at localhost:5432"
   → PostgreSQL is not running
   → Run: START_POSTGRES_NOW.bat
   → Or manually start from Services

❌ "Port 5000 already in use"
   → Another process is using port 5000
   → Kill it: taskkill /F /IM node.exe
   → Or change PORT in server/.env

❌ "Port 3000 already in use"
   → Another process is using port 3000
   → Kill it: taskkill /F /IM node.exe
   → Or change port in client package.json

❌ "Module not found" errors
   → Dependencies not installed
   → Run: npm install (in both server and client folders)

❌ "Chatbot not responding"
   → Check OpenAI API key in server/.env
   → Verify server is running on port 5000
   → Check browser console for errors

IMPORTANT NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ PostgreSQL must be running BEFORE starting the server
✓ Keep both terminals (server and client) open while developing
✓ Server runs on: http://localhost:5000
✓ Client runs on: http://localhost:3000
✓ Database: simuai_db (PostgreSQL)
✓ Chatbot uses OpenAI GPT-4o API

QUICK COMMANDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Start PostgreSQL:
  net start postgresql-x64-16

Stop PostgreSQL:
  net stop postgresql-x64-16

Test database connection:
  cd server
  node -e "const {PrismaClient} = require('@prisma/client'); const p = new PrismaClient(); p.$connect().then(() => console.log('✅ Connected')).catch(e => console.log('❌', e.message))"

View server logs:
  tail -f server/logs/combined.log

Clear npm cache:
  npm cache clean --force

Reinstall dependencies:
  cd server && npm install
  cd client && npm install

FEATURES READY TO USE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ User Authentication (Register/Login)
✅ Job Listings & Applications
✅ Interview System with AI Questions
✅ AI Chatbot (bottom-right corner)
✅ Performance Reports
✅ Anti-Cheat Monitoring
✅ Dashboard Analytics
✅ Payment Integration
✅ Email Notifications
✅ Admin Panel

NEXT STEPS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ Start PostgreSQL
2. ✅ Start Server (npm run dev in server folder)
3. ✅ Start Client (npm run dev in client folder)
4. ✅ Open http://localhost:3000
5. ✅ Register and explore!

═══════════════════════════════════════════════════════════════════════════════

For detailed information, see: COMPLETE_STARTUP_GUIDE.md
For database help, see: START_DATABASE_NOW.md
For PostgreSQL help, see: POSTGRESQL_STARTUP_GUIDE.txt

═══════════════════════════════════════════════════════════════════════════════
